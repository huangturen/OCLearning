#import 可以保证文件只被加载一次

Foundation头文件近1M，Xcode非常聪明，使用预编译头文件，在通过#import导入这种文件时，加载速度会非常快

NS代表定义的该事物来自于Cocoa，因此，自定义的变量或函数就不要加“NS”

BOOL是一种对带符号的字符类型（signed char）的定义，只占八位，当将长于1字节且低位为00000000的变量赋给BOOL时会出错。


